# Copy to .env.local for local development. Do not commit real values.

# Base URL of the UpdateWatch2 server API. Leave empty to call the API on
# the same origin the frontend is served from (the expected production
# setup once both are behind one reverse proxy).
VITE_API_BASE_URL=https://localhost:8443
